/**
 * Option A: Rename/copy this file to mapbox-config.js and set your Mapbox *public* token (pk.…).
 * Option B: Commit data/mapbox-config.js to the repo if your org allows it (pk. tokens are client-visible anyway).
 * Flash to the device: pio run -t uploadfs (SPIFFS must contain /mapbox-config.js).
 *
 * Mapbox → Account → Tokens: allow URLs you use (e.g. http://192.168.0.0/8) if the map fails to load styles.
 */
window.MESHEMS_MAPBOX_TOKEN = "";
